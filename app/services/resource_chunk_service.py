# Tables: resource_chunks

class ResourceChunkService:
    def create_chunks(resource_id, text)
    def get_chunks_by_resource(resource_ids)
    def vector_search(resource_ids, query_embedding, top_k)
