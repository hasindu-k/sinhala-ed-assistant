# Tables: messages

class MessageService:
    def create_user_message(session_id, content, modality)
    def create_system_message(session_id, content)
    def create_assistant_message(session_id, content, model_info)
    def list_session_messages(session_id)
